## 1. Modelagem dos dados do relatório

- [x] 1.1 Criar `frontend/src/app/report/report-data.model.ts` com as interfaces TypeScript (`Kpi`, `MarketStat`, `Comparable`, `TargetEstimate`, `BusinessPotential`, `AppendixItem`, `Methodology`, `NextStep`, etc.) e verificar que o projeto compila (`npx tsc --noEmit -p frontend/tsconfig.app.json` ou `npx ng build` dentro de `frontend/`)
- [x] 1.2 Criar `frontend/src/app/report/report-data.ts` exportando `REPORT_DATA`, transcrevendo do `Relatorio_Ilha_Aratuba_Vera_Cruz.pdf`: sumário executivo/KPIs, metodologia + fontes + limitações, estatística oficial do portal, estatística recalculada, lista de outliers da Seção 3.3, e verificar visualmente cada campo contra o PDF (conferência linha a linha)
- [x] 1.3 Adicionar ao `REPORT_DATA` a tabela de ~13 comparáveis da Seção 4 (preço, área, quartos, R$/m², observação, `sourceLabel`, `sourceUrl`) e verificar que todos os 13 links de anúncio abrem a página correta do portal (ZAP/Wimoveis) referenciada no PDF — URLs extraídas das anotações de link (hyperlinks) reais embutidas no PDF via `pypdf`, não inferidas
- [x] 1.4 Adicionar ao `REPORT_DATA` a estimativa preliminar do imóvel-alvo (premissas + faixa conservadora/otimista da Seção 5) e a seção de potencial de negócio/pousada (considerações a favor + pontos de atenção da Seção 6) e verificar que nenhum texto de aviso/ressalva do PDF foi omitido
- [x] 1.5 Adicionar ao `REPORT_DATA` o apêndice completo de ~22 imóveis da Seção 8, com `isOutlier: true` nas linhas destacadas em âmbar no PDF, e os próximos passos recomendados da Seção 7 na mesma ordem do relatório, e verificar a contagem (22 itens no apêndice, passos na ordem 1–5) — contagem conferida por script: 22 itens no apêndice, 4 marcados como outlier (os 2 outliers restantes da Seção 3.3 vêm de imobiliárias locais/Nível A que não fazem parte da base ZAP+Wimoveis do apêndice, então não têm linha correspondente para marcar)

## 2. Componente de página única

- [x] 2.1 Gerar o componente standalone `ReportPage` em `frontend/src/app/report/` (`report-page.ts`/`.html`/`.scss`, seguindo o padrão de nomenclatura 2025 já usado no projeto, sem sufixo `.component`) e verificar que ele renderiza sem erros via `npx ng build`
- [x] 2.2 Em `app.html`, substituir o `<router-outlet />` por `<app-report-page />`; esvaziar `app.routes.ts` (`export const routes: Routes = []`); e verificar que a aplicação sobe com `npx ng serve` e mostra o componente de relatório na raiz (`/`)
- [x] 2.3 Implementar o cabeçalho fixo (`mat-toolbar`) com o título do relatório e o card de KPIs sempre visível no topo (nº de anúncios, R$/m² de mercado, faixa de preço observada, faixa estimada do imóvel-alvo), consumindo `REPORT_DATA`, e verificar visualmente (screenshot em viewport 375×812) que os 4 KPIs aparecem sem precisar expandir nada

## 3. Seções de conteúdo em accordion

- [x] 3.1 Implementar `mat-accordion` com paineis para Metodologia/Fontes/Limitações e Estatísticas de Mercado (oficial + recalculada, claramente diferenciadas), consumindo `REPORT_DATA`, e verificar que os dois conjuntos de estatística não se misturam visualmente
- [x] 3.2 Implementar o painel de Comparáveis como lista de `mat-card` (um por imóvel, campos empilhados + botão/link "Ver anúncio original" com `target="_blank" rel="noopener"` quando `sourceUrl` existir, sem elemento de link quando não existir) e verificar em viewport 375px que nenhum card provoca rolagem horizontal e que os links abrem em nova aba — verificado via script no navegador: 13 cards, 13 links, todos `target="_blank"`/`rel="noopener"`, sem overflow horizontal em 375px
- [x] 3.3 Implementar o painel de Estimativa do Imóvel-Alvo (premissas + faixa conservadora/otimista + aviso de natureza preliminar) e o painel de Potencial de Negócio (considerações a favor + pontos de atenção), e verificar que o aviso de "estimativa preliminar sujeita a confirmação de área construída" está visível no painel de estimativa
- [x] 3.4 Implementar o painel de Apêndice como lista de `mat-card` com badge de aviso ("Outlier", estilizado com os tokens `--mat-sys-error-container`/`--mat-sys-on-error-container` do tema — trocado de `mat-chip` para um badge simples porque os CSS custom properties do MDC chip não aplicavam a cor visualmente) nos itens com `isOutlier: true`, e o painel de Próximos Passos como lista numerada — contagem real de outliers no apêndice é **4**, não 6: os outros 2 outliers da Seção 3.3 (R$ 1.100.000 Fábio Braga e R$ 3.980.000 Cond. Ponta da Ilha) vêm de imobiliárias de Nível A que não fazem parte da base ZAP+Wimoveis listada no apêndice, então não têm linha para marcar — essa correção de contagem já estava anotada na tarefa 1.5

## 4. Responsividade mobile-first e estilo

- [x] 4.1 Aplicar CSS mobile-first em `report-page.scss` (regras base para largura estreita, `@media` apenas para ampliar em telas ≥768px — ex.: grid de 2–3 colunas nos cards de comparáveis/apêndice) e verificar em viewports 375px, 768px e 1280px que não há rolagem horizontal em nenhum deles — confirmado por script (`document.body.scrollWidth <= clientWidth`) nos três viewports
- [x] 4.2 Revisar contraste e tamanho de toque dos elementos interativos (links de anúncio, cabeçalhos do accordion) sobre o tema `azure-blue` já configurado, e verificar visualmente em modo claro que o texto permanece legível — cabeçalhos do accordion com 48px de altura, botões de link com a área de toque padrão do Material (40px visível + touch target ampliado), texto do toolbar em alto contraste (quase preto sobre quase branco)

## 5. Verificação final

- [x] 5.1 Rodar `npx ng build` em `frontend/` e confirmar que o build de produção completa sem erros nem warnings novos — build final limpo (357.52 kB raw / 86.89 kB transferência estimada, sem warnings de budget)
- [x] 5.2 Abrir o app no navegador emulando um viewport mobile (ex.: 390×844) e conferir manualmente, seção por seção, que todo conteúdo obrigatório da spec (`market-report-viewer`) está presente e que os links de comparáveis apontam para as URLs corretas do PDF — todas as 7 seções (Metodologia, Estatísticas, Comparáveis, Estimativa, Potencial de Negócio, Próximos Passos, Apêndice) mais o card de KPIs renderizam corretamente; os 35 links (13 comparáveis + 22 apêndice) batem 1:1, na mesma ordem, com as URLs reais extraídas das anotações de hyperlink do PDF
